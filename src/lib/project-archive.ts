export * from '../../packages/review/src/project-archive'

/*
 * The implementation lives in the client-independent Review workspace. This
 * compatibility entry point keeps existing Web imports stable during the
 * incremental migration.
 */
