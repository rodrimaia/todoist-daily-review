# Web client

The Web client remains the existing Vite application during the incremental
monorepo migration. Its public routes, static output, deployment files, and
browser-owned cache/session behavior remain at the repository root until the
shared Review extraction is complete.
